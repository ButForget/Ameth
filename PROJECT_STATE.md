The repo now supports `ameth <name> [path]` for project initialization and `ameth ideas` for basic idea management.
Initialization now creates the full root layout, and the ideas workflow supports creating, listing, showing, abandoning, and restoring idea files.
