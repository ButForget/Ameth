The repo now supports `ameth <name> [path]` for project initialization and `ameth ideas` for idea management.
Initialization now creates the full root layout plus `Ameth.toml`, and the ideas workflow supports creating, listing, showing, pinning, abandoning, and restoring idea files.
