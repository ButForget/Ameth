The repo now supports `ameth init <name> [path]`, `ameth ideas`, and `ameth rq`.
Initialization now creates the full root layout plus `Ameth.toml` and root `ResearchQuestion.md`, the ideas workflow supports creating, listing, showing, pinning, abandoning, restoring, and editor-assisted creation of idea files, and `rq` supports showing and editing the free-form research-question file.
